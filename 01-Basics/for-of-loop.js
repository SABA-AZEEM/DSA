//for-of-loop: It's used to iterating over iterable objects like arrays,strings,maps,sets etc.
//it allows you to loop through the values directly without needing to access the indices.
const fruits = ['apple', 'banana', 'orange'];

for(const fruit of fruits){
    console.log("Array:"+ fruit);
}
